<Window x:Class="ModalCalendarNotification.UI.Features.CalendarSelection.CalendarListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calendar Selection"
        Width="500"
        Height="360"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <ListView ItemsSource="{Binding Items}">
      <ListView.View>
        <GridView>
          <GridViewColumn Header="Selected" Width="80">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" HorizontalAlignment="Center" />
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
          <GridViewColumn Header="Provider" DisplayMemberBinding="{Binding ProviderName}" Width="140" />
          <GridViewColumn Header="Calendar" DisplayMemberBinding="{Binding DisplayName}" Width="220" />
        </GridView>
      </ListView.View>
    </ListView>

    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Width="90" Margin="0,0,8,0" Content="Load" Command="{Binding LoadCommand}" />
      <Button Width="90" Content="Save" Command="{Binding SaveCommand}" />
    </StackPanel>
  </Grid>
</Window>
