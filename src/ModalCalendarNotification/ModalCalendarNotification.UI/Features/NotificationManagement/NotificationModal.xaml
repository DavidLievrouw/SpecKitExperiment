<Window
  x:Class="ModalCalendarNotification.UI.Features.NotificationManagement.NotificationModal"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  Title="Calendar Notification"
  Width="600"
  Height="400"
  ResizeMode="NoResize"
  WindowStartupLocation="CenterScreen"
  Topmost="True"
>
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,12" />

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Events}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,0,12" Padding="12" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4">
              <StackPanel>
                <!-- Event Title and Time -->
                <Grid Margin="0,0,0,8">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <StackPanel Grid.Column="0">
                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" />
                    <TextBlock
                      Text="{Binding StartUtc, StringFormat='Starts: {0:ddd h:mm tt}'}"
                      FontSize="12"
                      Foreground="Gray"
                      Margin="0,4,0,0"
                    />
                    <!-- Provider and Account Label -->
                    <TextBlock
                      Text="{Binding ProviderAccountLabel}"
                      FontSize="11"
                      Foreground="DarkGray"
                      Margin="0,2,0,0"
                    />
                  </StackPanel>
                </Grid>

                <!-- Per-Event Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                  <Button
                    Content="Dismiss"
                    Padding="8,4"
                    Margin="0,0,4,0"
                    Command="{Binding DataContext.DismissSingleEventCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                  />
                  <Button
                    Content="Snooze"
                    Padding="8,4"
                    Margin="0,0,4,0"
                    Command="{Binding DataContext.SnoozeSingleEventCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                  />
                  <Button
                    Content="Dismiss All Future"
                    Padding="8,4"
                    Command="{Binding DataContext.DismissAllFutureCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    CommandParameter="{Binding}"
                  />
                </StackPanel>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <!-- Global Action Buttons -->
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button Width="90" Margin="0,0,8,0" Content="Snooze All" Command="{Binding SnoozeCommand}" />
      <Button Width="90" Content="Dismiss All" Command="{Binding DismissCommand}" />
    </StackPanel>
  </Grid>
</Window>
